# GPT

Use `../../knowledge/` como fonte canônica. Estado específico do GPT pode ficar aqui. O histórico detalhado V1 em `../../GPT/` permanece preservado durante a migração.

Não grave chain-of-thought privado; somente Decision Traces permitidos em `../../reflections/`.
