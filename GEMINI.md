# Gemini CLI — Memórias IA Infinity

A Central V2 usa uma única fonte canônica. Carregue as regras compartilhadas por import:

@./AGENTS.md

Depois consulte `knowledge/INDEX.md` e apenas os arquivos relevantes ao pedido. Use `agents/gemini/` somente para estado específico do Gemini. Memória persistida pelo agente não deve ganhar autoridade canônica automaticamente.
