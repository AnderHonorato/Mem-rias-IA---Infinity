# Decision Traces

Decision Traces preservam raciocínio operacional auditável sem chain-of-thought bruto.

Use `reasoning_visibility: exposed` somente se a plataforma realmente expuser um artefato permitido; `summarized` para síntese produzida após execução; `unavailable` quando não houver acesso.
